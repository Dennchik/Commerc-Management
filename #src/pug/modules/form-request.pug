form(action="#" id='form-request').form-request
	._body
		._btn-close.btn-close
			+iconfont('plus', '_icon')
		._title.t-h2 Оставить заявку
		._content 
			._row
				._column
					._line
						label( for="name")._lebel
						._input
							input.input.name(id="name" placeholder="Имя*" autocomplete="off",
							type="text"  name="name"  error="Обязательное поле")
				._column
					._line
						label(for="phone")._lebel
						._input
							input.input(type="tel" class="phone" id='phone' placeholder="+7 (___) ___-__-__" autocomplete="off" name="phone" error="Обязательное поле" 
							)
				._column
					._line
						label(for="e-mail")._lebel
						._input
							input.input(type="tel" class="maile" id='e-mail' placeholder="e-mail" autocomplete="off" name="mail" error="Обязательное поле" 
							)
				._column 
					.checkbox
						input(type="checkbox" id="call" name="agree" form="form-request")._input 
						label(for="call")._label 
							+iconfont('check', '_icon')
							span Подтверждаю, что&nbsp;ознакомился&nbsp;(&#8209;лась) с&nbsp;политикой конфиденциальности и&nbsp;даю&nbsp;согласие на&nbsp;обработку персональных 
			._row 
				._button
					button(form="form-request" type="button" disabled="disabled").button.t-h2
						span Оставить заявку